{
  "name": "Copy Page Title and URL",
  "version": "1.0",
  "description": "A Chrome extension that allows you to copy both the page title and URL using the keyboard shortcut Ctrl+Shift+C.",
  "permissions": ["clipboardWrite", "tabs"],
  "background": {
    "scripts": ["background.js"],
    "persistent": false
  },
  "content_scripts": [{
    "matches": ["<all_urls>"],
    "js": ["content.js"]
  }],
  "manifest_version": 2,
  "commands": {
    "copy-to-clipboard": {
      "suggested_key": {
        "default": "Ctrl+Shift+P"
      },
      "description": "Copy page title and URL to clipboard"
    }
  },
  "web_accessible_resources": [
    "content.js",
    "background.js"
  ]
}